
/*
*ITESS-TICS 20225 Estructura y Organización de Datos
*Agosto Dicimebre 2025
*Docente:Francisco Javier Montecillo Puente
*Programador: Martinez Aayala Jorge Salvador, Aguilar Hernandez Oliver
*Fecha:09/12/2025
 */
package tema4.ordenamientobusqueda;
import java.util.Random;

public class BusquedaLineal {
    public static void main(String[] args) {
        int[] datos = new int[1000000];
        Random rand = new Random();
        int target = 123456;

        for (int i = 0; i < datos.length; i++) {
            datos[i] = rand.nextInt(1000000);
        }

        boolean encontrado = false;
        for (int i = 0; i < datos.length; i++) {
            if (datos[i] == target) {
                encontrado = true;
                System.out.println("Elemento encontrado en la posición: " + i);
                break;
            }
        }

        if (!encontrado) {
            System.out.println("Elemento no encontrado.");
        }
    }
}
